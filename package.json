{
  "name": "resume",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "yarn build",
    "watch": "yarn serve & yarn w-css",
    "serve": "browser-sync start --server --serveStatic dist --files 'dist/*.css, index.html'",
    "css": "postcss ./src/resume.css -o ./dist/resume.css --no-map",
    "w-css": "postcss ./src/resume.css -o ./dist/resume.css -w -m",
    "lint-css": "stylelint ./src/**/*.css",
    "build": "postcss ./src/resume.css -o ./dist/resume.css && cpx ./index.html build/ && cpx ./dist/**.css build/dist/ && cpx ./static/**.png build/static/",
    "deploy": "yarn build && gh-pages -d build",
    "heroku-prebuild": "yarn"
  },
  "author": "hrfmmymt",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^7.2.3",
    "browser-sync": "^2.19.0",
    "cpx": "^1.5.0",
    "gh-pages": "^1.1.0",
    "postcss": "^6.0.14",
    "postcss-cli": "^4.1.1",
    "postcss-custom-properties": "^6.2.0",
    "postcss-import": "^11.0.0",
    "postcss-nested": "^3.0.0",
    "stylefmt": "^6.0.0",
    "stylelint": "^8.4.0"
  },
  "dependencies": {
    "express": "4.16.2"
  }
}
